{
  "repository": "https://github.com/jmcarp/feedback",
  "addons": ["heroku-redis:hobby-dev"],
  "env": {
    "GITHUB_USER": {
      "description": "GitHub user who owns the target repo",
      "required": true
    },
    "GITHUB_REPO": {
      "description": "GitHub repo to submit issues to",
      "required": true
    },
    "GITHUB_TOKEN": {
      "description": "https://frozen-dusk-81360.herokuapp.com",
      "required": true
    },
    "GITHUB_CLIENT_ID": {
      "description": "Client ID from GitHub developer application",
      "required": true
    },
    "GITHUB_CLIENT_SECRET": {
      "description": "Client secret from GitHub developer application",
      "required": true
    },
    "BASE_URL": {
      "description": "Base URL of feedback application",
      "required": true
    },
    "ORIGIN_URL": {
      "description": "Base URL of submitting application",
      "required": true
    }
  }
}
